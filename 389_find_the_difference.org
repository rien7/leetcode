:PROPERTIES:
:ID:       a09298a1-221f-4eef-b436-2ef45d8ccf49
:ROAM_REFS: https://leetcode.com/problems/find-the-difference/submissions/
:END:
#+title: 389. Find the Difference
#+filetags: :Easy:

* [[id:2b2e3e1f-924b-43fe-a7f8-a1eb6482d2f2][问题]]
You are given two strings ~s~ and ~t~.

String ~t~ is generated by random shuffling string ~s~ and then add one more letter at a random position.

Return the letter that was added to ~t~.

*Example 1:*

#+BEGIN_SRC
Input: s = "abcd", t = "abcde"
Output: "e"
Explanation: 'e' is the letter that was added.
#+END_SRC

Example 2:

#+BEGIN_SRC
Input: s = "", t = "y"
Output: "y"
#+END_SRC

*Constraints:*

- ~0 <= s.length <= 1000~
- ~t.length == s.length + 1~
- ~s~ and ~t~ consist of lowercase English letters.

* 思路
今天的题目比较的简单, 是经典的字母频率记录的问题, 用数组进行记录即可.

注意这道题与字母出现的顺序无关, 只与字母出现的频率有关

* 代码
#+BEGIN_SRC go
func findTheDifference(s string, t string) byte {
    m := make([]int, 26)
    n := len(s)
    m[t[n]-97]++
    for i := 0; i < n; i++ {
        m[t[i]-97]++
        m[s[i]-97]--
    }
    for i, v := range m {
        if v == 1 { return byte(i+97) }
    }
    return 0
}
#+END_SRC
